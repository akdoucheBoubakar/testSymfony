<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel ="stylesheet" href="http://bootswatch.com/4/flatly/bootstrap.min.css">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="/">My_Project </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse"
                data-target="#navbarColor01" aria-controls="navbarColor01"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path ('liste_Place')}}">liste places <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{  path('liste_appointment')}}">liste Appointement</a>
                </li>
            <li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path ('liste_customer')}}">liste Customer <span class="sr-only">(current)</span></a>
                </li>
            </li>

               {% if not app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('page_authentification') }}">deconnection </a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('inscription') }}">deconnectio</a>
                </li>
                {% endif %}

              </ul>

          </div>
    </nav>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
